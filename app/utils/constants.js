// app/utils/constants.js

/**
 * Penalty rate applied when banking winnings.
 *
 * Example: If BANK_PENALTY_RATE = 0.13 → 13% is deducted
 * from the banked amount when transferring to balance.
 */
export const BANK_PENALTY_RATE = 0.13;
